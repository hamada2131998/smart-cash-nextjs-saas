# UX LITE HOME UPDATES

## ما تم تنفيذه

- إنشاء Lite Home موحدة لكل الأدوار في `src/app/dashboard/home/page.tsx`.
- إضافة Welcome State لأول مرة لـ `owner/admin` فقط باستخدام localStorage key scoped per user/company:
  - `smartcash_welcome_dismissed_{userId}_{companyId}`
- إضافة 3 خطوات البداية داخل Welcome:
  - دعوة موظف جديد
  - إضافة عهدة أولى
  - جرّب إضافة مصروف
- تحديث عنوان الرصيد إلى:
  - `🔵 رصيد العهدة المتاح للصرف`
- إضافة عداد `طلبات معلّقة` للأدوار الإدارية من نفس مصدر pending inbox المستخدم في الهيدر (`custody_transactions` pending received requests).
- جعل أعلى Lite Home عبارة عن 3 CTA كبيرة (mobile-first):
  - إضافة مصروف
  - طلب تغذية رصيد
  - عرض كل الأقسام
- تطبيق Progressive Disclosure بإضافة صفحة روابط أقسام:
  - `src/app/dashboard/all/page.tsx`
  - الروابط المتقدمة لا تظهر داخل Lite Home مباشرة.
- إضافة زر Demo للأدوار `owner/admin` فقط:
  - `جرّب النظام في 30 ثانية` (Modal إرشادي بدون كتابة بيانات).
- إضافة شريط تنبيه بسيط عند وجود طلبات pending:
  - `🔴 لديك {n} طلب ينتظر الموافقة` + زر مراجعة.
- تحديث نص زر الدعوة في المستخدمين:
  - من `إضافة مستخدم جديد` إلى `دعوة موظف جديد` في `src/app/dashboard/users/page.tsx`.
- إضافة صفحة تعريفية:
  - `src/app/why/page.tsx`
  - وربطها من Lite Home عبر `لماذا هذا النظام؟`.

## وصف UI (بديل اللقطات)

- Lite Home الآن شاشة بسيطة جدًا تركّز على:
  1) الرصيد
  2) 3 إجراءات رئيسية
  3) حركات خفيفة في الأسفل
- للموظف/مندوب المبيعات:
  - زر `القائمة` يفتح روابط minimal فقط.
- للمالك/الإداري:
  - يظهر Welcome State أول مرة
  - يظهر زر Demo Flow
  - يظهر عداد الطلبات المعلقة بجانب الرصيد.

## QA

- `npm run build`: PASS
- Route checks من build:
  - `/dashboard/home`
  - `/dashboard/all`
  - `/why`

## TODOs

- تنفيذ تحقق role-based كامل عبر جلسات اختبار حقيقية (owner/employee/sales_rep) على بيئة تشغيل تفاعلية.
- إذا لاحقًا تم تغيير مصدر pending counter في الهيدر، يجب توحيد المصدر في Lite Home بنفس الشكل.
